#!/usr/bin/env bash
# ==============================================================================
# Talbica Replica - 开发环境启动脚本 (Dev Script)
# ==============================================================================

set -e

PORT=${PORT:-5173}
HOST=${HOST:-"0.0.0.0"}

echo "🚀 启动 Talbica 开发服务器 (http://${HOST}:${PORT})..."
npx vite --host "${HOST}" --port "${PORT}"
