version: '3.8'

services:
  talbica:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: talbica-app
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
